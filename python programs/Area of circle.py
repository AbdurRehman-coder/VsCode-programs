radius = input('Enter the radius of circle')
radius = float(radius)
area = 3.14 * (radius * radius)
print('the area of circle is %f' %area)   #%f is for floating value
print('the area of circle with radius %f is %f' %(radius,area)) # first %f store the radius and 2nd %f store the area value
